generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model errevents {
  id           Int       @id @default(autoincrement())
  errorid      String?
  datetime     DateTime?
  userid       String?
  errortype    String?
  errormessage String?
  stack        String?
  userparam    String?
  usernote     String?
}
