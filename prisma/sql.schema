DROP TABLE IF EXISTS errevents;

CREATE TABLE errevents (
    id SERIAL PRIMARY KEY,
    message varchar(1000),
    name varchar(1000),
    stack TEXT,
    clientid varchar(1000),
    userparam varchar(1000),
    usernote varchar(1000)
    );

